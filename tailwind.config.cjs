const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		letterSpacing: {
			wider: '0.25em',
			widest: '0.5em'
		},
		fontFamily: {
			display: ['Alexandria', 'sans-serif']
		},
		extend: {}
	},

	plugins: []
};

module.exports = config;
